import { PartialType } from '@nestjs/swagger';
import { CreateStringGeesetupDto } from './create-string-geesetup.dto';

export class UpdateStringGeesetupDto {
   roomId: string; // ID phòng cần cập nhật (nếu cần)
 }
